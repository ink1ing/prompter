# 🚀 Prompter 快速开始指南

## 第一步:启动程序

```bash
# 方式1:使用启动器(推荐)
./click_me_stable.command
# 然后选择 2 (标准模式)

# 方式2:直接命令
./target/release/prompter --telegram-listen
```

## 第二步:激活Telegram机器人 ⚠️ 重要!

**如果看到"无法获取Chat ID"错误,需要先激活机器人:**

1. 打开Telegram
2. 搜索你的机器人(例如:`@prompter114514_bot`)
3. 点击"START"或发送 `/start` 命令
4. 机器人会自动获取你的Chat ID

## 第三步:测试功能

### 1. 测试Telegram命令

在Telegram中发送:
```
/help
```

你会收到帮助信息,包含所有可用命令。

### 2. 配置反馈间隔

```
/based-on-number 50
```
设置每50个提示词推送一次反馈

### 3. 查看状态

```
/status
```

### 4. 观察终端输出

程序会实时显示检测到的提示词:

```
[21:45:32] 200 ✅ 记录成功 | 请帮我分析这个代码的性能问题
[21:45:38] 200 ✅ 记录成功 | 如何优化这段代码...
[21:45:41] 200 ✅ 记录成功 | 能帮我重构这段代码吗
```

## 工作原理

```
┌─────────────────────────────────────┐
│    标准模式 - Gemini智能分析        │
└─────────────────────────────────────┘
            ↓
    ┌──────────────┬──────────────┐
    ↓              ↓              ↓
监控Shell历史   Telegram监听   本地保存
    ↓              ↓              ↓
检测中文提示词  接收命令设置   data/prompts.md
    ↓              ↓
[时间戳] 200 ✅  配置反馈间隔
```

## 可用命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/based-on-time <小时>` | 时间间隔 | `/based-on-time 12` |
| `/based-on-number <数量>` | 数量间隔 | `/based-on-number 100` |
| `/prompt <系统提示词>` | 自定义AI分析提示词 | `/prompt 你是专业的提示词优化专家...` |
| `/status` | 查看状态 | `/status` |
| `/report` | 生成总体报告 | `/report` |
| `/help` | 帮助信息 | `/help` |

### /prompt 命令详解 (v2.4新增)

`/prompt` 命令允许你自定义Gemini分析你的提示词时使用的系统提示词。

**用法:**
```
/prompt 你是一位专业的AI提示词优化专家，专门负责审视用户与编码模型Claude Code的交互。请重点关注...
```

**默认系统提示词包含:**
- 评估提示词质量(清晰度、完整性、有效性)
- 给出改进建议
- 肯定优秀实践
- 遵循LLM最佳实践(具体性、结构性、上下文、约束条件、示例)
- 使用中文回复,200-400字,Markdown格式

**修改后:**
- 配置会自动保存到 `config.toml`
- 重启程序后生效
- 新提示词将用于所有后续AI分析

## 数据保存位置

- **提示词记录**: `./data/prompts.md`
- **配置文件**: `~/.prompter/feedback_interval.json`
- **主配置**: `config.toml`

## 常见问题

### Q: 看到"无法获取Chat ID"?
**A:** 在Telegram中向机器人发送 `/start` 命令

### Q: 机器人不回复?
**A:** 检查:
1. 程序是否正在运行 (`ps aux | grep prompter`)
2. 是否已发送 `/start` 激活机器人
3. Token是否正确

### Q: 没有检测到提示词?
**A:**
1. 确保在终端执行了包含中文的命令
2. 等待3秒刷新间隔
3. 中文字符需要≥10个

### Q: 如何停止程序?
**A:** 按 `Ctrl+C`

## 下一步

✅ 程序运行中,继续使用Claude Code
✅ 每个中文提示词都会被记录并显示200状态
✅ 达到设定间隔后自动推送AI分析

🎉 享受智能化的AI交互体验!
