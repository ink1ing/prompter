# Prompter 自动化使用指南

## 🎯 功能概述

Prompter 现在支持高度自动化的中文提示词收集和上传功能：

- ⏰ **定时自动上传**: 每1小时自动将中文交互上传到Cloudflare网站
- 🔍 **智能中文检测**: 自动识别和过滤中文提示词
- 🌐 **Cloudflare集成**: 直接上传到你的Cloudflare Pages网站
- 📱 **GitHub触发**: 自动触发GitHub Pages重新部署
- 💾 **自动备份**: 本地数据自动备份，防止丢失

## 🚀 快速开始

### 1. 一键配置启动

```bash
# 运行自动化配置向导
./auto_start.sh
```

这个脚本会：
- 自动编译项目
- 引导你配置Cloudflare信息
- 生成Pages Function代码
- 测试配置
- 启动自动化服务

### 2. 手动配置步骤

如果你喜欢手动配置：

#### 步骤1: 编译项目
```bash
cargo build --release
```

#### 步骤2: 配置 config.toml
```toml
[cloudflare]
account_id = "你的Account ID"
zone_id = "你的Zone ID"
api_token = "你的API Token"

[website]
domain = "你的域名.com"
github_repo = "username/repo-name"
```

#### 步骤3: 启动自动化
```bash
./target/release/prompter --auto
```

## 📋 使用模式

### 自动化模式（推荐）
```bash
# 启动定时自动上传
./target/release/prompter --auto

# 后台运行
nohup ./target/release/prompter --auto > prompter.log 2>&1 &
```

### 手动上传模式
```bash
# 立即上传当前的中文提示词
./target/release/prompter --upload
```

### 简化监控模式
```bash
# 只收集提示词，不自动上传
./target/release/prompter --simple
```

## 🌐 Cloudflare 配置

### 1. 获取必要信息

在 [Cloudflare 仪表板](https://dash.cloudflare.com) 中获取：

- **Account ID**: 右侧边栏中显示
- **Zone ID**: 域名概览页面右侧
- **API Token**: 创建自定义令牌，需要 `Zone:Edit` 权限

### 2. 设置 Pages Function

将生成的 `functions/api/prompts.js` 文件上传到你的 Cloudflare Pages 项目：

```bash
# 项目结构
your-cloudflare-pages-project/
├── functions/
│   └── api/
│       └── prompts.js
└── ...其他文件
```

### 3. 配置 KV 存储

1. 在 Cloudflare 仪表板中创建 KV 命名空间
2. 将命名空间绑定到 Pages 项目，绑定名称: `PROMPTER_KV`

## 📊 数据流程

```
Prompter 收集提示词
    ↓
智能过滤中文内容
    ↓
每小时批量打包
    ↓
上传到 Cloudflare KV
    ↓
触发 GitHub 部署
    ↓
网站自动更新
```

## 🔧 配置选项详解

### 中文过滤配置
```toml
[filter]
detect_chinese = true        # 启用中文检测
min_chinese_chars = 3       # 最少中文字符数
exclude_commands = ["/", "quit", "exit", "help"]  # 排除的命令
```

### 自动化配置
```toml
[app]
auto_upload_enabled = true   # 启用自动上传
upload_interval_hours = 1    # 上传间隔（小时）
```

### 存储配置
```toml
[storage]
data_dir = "./data"         # 数据目录
prompts_file = "prompts.md" # 提示词文件
backup_dir = "./backups"    # 备份目录
max_backups = 10           # 最大备份数量
```

## 📈 监控和调试

### 查看日志
```bash
# 实时日志
tail -f prompter.log

# 查看最近上传
grep "成功上传" prompter.log
```

### 手动测试
```bash
# 测试 Cloudflare 连接
./target/release/prompter --upload

# 查看帮助
./target/release/prompter --help
```

### 常见问题

**Q: 上传失败怎么办？**
A: 检查网络连接和API Token权限，查看详细错误日志

**Q: 如何修改上传间隔？**
A: 编辑 `config.toml` 中的 `upload_interval_hours` 参数

**Q: 如何停止自动化服务？**
A: 按 `Ctrl+C` 或使用 `kill` 命令停止后台进程

## 🎉 完整示例

假设你的网站是 `prompts.example.com`，GitHub仓库是 `username/my-prompts`：

1. 运行配置向导：
```bash
./auto_start.sh
```

2. 输入配置信息：
- 域名: `prompts.example.com`
- GitHub: `username/my-prompts`
- 上传间隔: `1` 小时

3. 启动自动化：
```bash
./target/release/prompter --auto
```

现在，每当你使用Claude Code时，所有中文提示词都会自动收集并每小时上传到你的网站！

## 🔒 安全注意事项

- API Token 具有高权限，请妥善保管
- 定期轮换 API Token
- 不要在公开仓库中提交 `config.toml`
- 考虑使用环境变量存储敏感信息

## 📚 进阶用法

### 环境变量配置
```bash
export CLOUDFLARE_API_TOKEN="your-token"
export CLOUDFLARE_ACCOUNT_ID="your-account-id"
```

### 服务化部署
```bash
# 使用 systemd (Linux)
sudo cp prompter.service /etc/systemd/system/
sudo systemctl enable prompter
sudo systemctl start prompter
```

### 多环境部署
为不同环境创建不同的配置文件：
- `config.toml` - 生产环境
- `config-dev.toml` - 开发环境
- `config-test.toml` - 测试环境

---

🎯 现在你就拥有了一个完全自动化的中文提示词收集和发布系统！